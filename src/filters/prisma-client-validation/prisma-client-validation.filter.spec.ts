import { PrismaClientValidationFilter } from './prisma-client-validation.filter';

describe('PrismaClientValidationFilter', () => {
  it('should be defined', () => {
    expect(new PrismaClientValidationFilter()).toBeDefined();
  });
});
